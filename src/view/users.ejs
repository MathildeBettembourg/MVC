<!doctype html>
<html lang="fr">
<head>
    <%- include('./header.ejs') %>
    <title> Users </title>
</head>
<body>
    <%- include('./navbar.ejs') %>
        <h1>Users</h1>
        <a href="/userInput">Créer</a>
    <p>
    <% for (let i = 0; i < users.length; i++) { %>
        <div class="card" style="width: 18rem;">
            <div class="card-header"><%= users[i].username%></div>
            <a href="/users/<%= users[i].id%>">Détails</a>
            <a href="/deleteUser/<%= users[i].id%>">Supprimer</a>
            <a href="/modifUser/<%= users[i].id%>">Modifier</a>
        </div>        
    <% } %>
    </br>
    </p>
</body>
</html>